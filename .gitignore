.build-dir/
.*-build-dir/
.ostree-repo/
.*-ostree-repo/
*.oci/
*.bundle
*.flatpak
/tmp/

# Token/credential files — never commit
.token
*.token

# Large upstream binaries — never commit
*.deb
*.AppImage

# Git worktrees (local dev isolation)
.worktrees
